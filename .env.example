# Turso Database (required for production, optional for dev)
# Local dev auto-falls back to local.db if these are unset
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here